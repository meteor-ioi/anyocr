# anyocr 架构经验与演进决策记录 (memory.md)

> 本文档用于固化 `anyocr` 在架构设计、技术选型与工程实践中吸取的核心经验与架构决策（ADR），供后续开发者与 AI Agent 继承与遵循，防止历史问题倒流。

---

## 一、 核心架构演进与经验反思 (Lessons Learned)

### 1. 引擎生命周期：显式实例优于隐式单例
* **历史教训**：早期方案仅暴露无状态顶层函数（`to_markdown` / `parse`），忽略了 ONNX 引擎每次冷启动加载 80MB+ 模型耗时高达 300~800ms 的问题。若强制用全局 static 单例，又会导致多线程无法隔离不同配置（例如同时运行 CoreML 实例与 CPU 实例）。
* **架构裁决**：
  * 采用**双层 API** 设计：
    * **底座层（工业级）**：显式 `Engine::new(config)` 实例，用户自主管理会话生命周期与并发复用；
    * **顶层（便捷式）**：保留 `anyocr::to_markdown(...)` 静态函数，内部通过 `std::sync::OnceLock` 维护默认单例，供轻量脚本直接使用。

---

### 2. 文档表达模型：多页页级结构是底线
* **历史教训**：原设计将 `ParsedDocument` 视为单张大图（仅有一组 `dimensions` 与平铺的 `boxes`）。但现实中 PDF/OFD 均为多页，直接导致：
  1. 多页相同坐标 `[x1, y1, x2, y2]` 产生空间碰撞，前端原图高亮卷帘联动彻底失效；
  2. 无法支持超长文档的流式逐页内存回收。
* **架构裁决**：
  * 引入 `PageResult`，每页独立持有 `page_index`、`dimensions`、`blocks` 和 `boxes`；
  * `ParsedDocument` 由 `Vec<PageResult>` 构成，全篇 Markdown 默认以 `\n\n---\n\n` 分页符连接。

---

### 3. PDF 扫描件与栅格化：清晰划分能力边界
* **历史教训**：`lopdf` 属于纯语法流解析库，**不具备矢量图形光栅化（Rasterization）渲染能力**。若文档是无文字层、纯矢量绘图的“假扫描件”，`lopdf` 无法像 Python `pdf2image` 那样渲染为位图。
* **架构裁决**：
  * **第一阶段（零外部 C 动态库基线）**：依靠 `lopdf` 抽取内嵌位图流（覆盖 90% 真实扫描件与发票）。若遇到纯矢量无图且无字页面，显式返回 `AnyOcrError::PdfError` 提示，绝不静默失败；
  * **第二阶段（高阶光栅化扩展）**：预留可选 `feature = "pdfium"`，仅在用户显式开启时链接光栅化后端，坚决维护默认工程“零系统依赖、轻量便携”的核心定位。

---

### 4. 类型系统与 Feature Gating 运行时安全
* **历史教训**：在 Cargo 特性裁剪时，如果 `DocumentFormat` 枚举包含 `Pdf`，但用户使用了 `--no-default-features`（未开启 `pdf` feature），若底层直接 `panic` 或报错模糊，属于严重 API 缺陷。
* **架构裁决**：
  * 枚举类型全局统一，不在类型上做破坏性条件编译；
  * 在 `src/error.rs` 中增设 `AnyOcrError::UnsupportedFormat(String)` 显式错误变体，运行时遇到未编译的 feature 友好提示开发者。

---

### 5. 开发节奏：垂直闭环优先于过早优化
* **历史教训**：原节奏将“分桶批处理（Bucket Batching）”放入 M1，却将最基础的“图像解码 (`ingestion/image.rs`)”推迟到 M3，导致 M1 无法执行任何端到端测试，且过早引入复杂分桶算法拖慢冒烟进度。
* **架构裁决**：
  * **M1 目标收敛为“单图跑通”**：实现 `image.rs` ➔ 迁移 Det/Rec/Table ➔ 跑通单图 baseline 冒烟测试；
  * **M2 推进版面与优化**：落地 `DocBlock` AST 启发式后处理 ➔ 叠加 Rec 长宽比分桶批处理（吞吐提升 2~3 倍）；
  * **M3 推进全格式多页覆盖**：多页 PDF 坏死层检测 ➔ 国标 OFD 流式解析；
  * **M4 业务回归与发布**：SensiDoc 卸载老代码 ➔ crates.io 发布。

---

### 6. AST 语义完备性与坐标统一
* **历史教训**：`Paragraph` 缺整体包围盒、`List` 缺少坐标、缺少插图与印章节点。
* **架构裁决**：
  * 坐标包围盒统一为 `[x1, y1, x2, y2]`（四元数组）；
  * `DocBlock` 包含 `Heading`、`Paragraph`、`Table`、`List`、`Image` 五大核心节点，均持有独立 `bbox`，无缝服务于高保真排版与敏感信息原图审计。

---

## 二、 关键代码规约与准则

1. **零 Python 依赖承诺**：任何新增功能禁止引入 Python、Paddle 或不可控的重量级 C++ 运行时；
2. **内存严控**：图像裁剪和张量变换必须避免无谓的内存克隆（Clone），单张大图处理后及时释放临时 Buffer；
3. **坐标系统原点规范**：左上角为 `(0.0, 0.0)`，坐标点映射必须严格经过 `ResizeInfo` 逆变换，保证在原始高分图像上的绝对精准。
