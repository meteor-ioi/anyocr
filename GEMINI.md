# anyocr 项目专有架构规范与工程知识 (Project Knowledge & Guidelines)

本文档由 Antigravity 自动加载，作为本项目后续开发、重构与功能扩展的常驻工程知识与指导规范。

---

## 核心工程准则 (Senior Engineering Guidelines)

### 1. 深度学习与 ONNX 推理引擎生命周期准则
- **显式生命周期控制**：ONNX 模型加载耗时极大（数百毫秒），核心底层必须以结构体（如 `Engine`）管理 Session，由调用方显式持有和复用，禁止在公共核心层设计无状态冷启动函数；
- **双层 API 范式**：
  - 工业级服务场景 ➔ 使用 `Engine::new(config)` 实例；
  - 快速脚本/CLI 场景 ➔ 使用顶层便捷函数（内部基于全局惰性单例 `OnceLock` 缓存）。
- **杜绝硬编码静态单例**：核心引擎绝不能硬编码全局死锁，必须支持多实例隔离运行（如同时创建 CPU 实例与 CoreML/CUDA 实例）。

### 2. 版式文档与视觉感知的数据模型准则
- **多页结构是底线**：PDF/OFD 等版式文档天然多页，数据结构必须以页为基本单元（`PageResult`），包含独立的 `page_index`、`dimensions`、`blocks` 和 `boxes`；
- **严禁全篇坐标平铺**：绝不可将多页文档平铺为单图坐标，否则不同页面的相同坐标会发生空间碰撞，导致前端原图高亮与卷帘对比完全失效；
- **AST 节点空间一致性**：所有语义节点（`Heading`, `Paragraph`, `Table`, `List`, `Image`）必须保留空间包围盒 `bbox: [f32; 4]`，坐标原点必须严格映射回原图物理像素。

### 3. 技术选型能力边界与容错熔断准则
- **清晰划分解析与渲染边界**：
  - 语法流解析库（如 `lopdf`）只负责文本抽取与内嵌图像流提取，**不具备矢量图形光栅化渲染能力**；
  - 针对纯矢量无文字的“假扫描件”，必须返回显式强类型错误（`AnyOcrError::PdfError`），严禁静默失败；
  - 高成本光栅化库（如 `pdfium`）必须以可选 Cargo Feature 隔离，不得污染基础库零系统依赖的便携性。
- **坏死文字层自适应熔断**：针对损坏 CMap 的 PDF，统计不可见字符与乱码比例，异常时自动熔断回退至扫描件图像 OCR。

### 4. Rust 库工程与 Feature Gating 规范
- **特性裁剪运行时安全**：当 `Cargo.toml` 提供可选特性（`pdf`, `ofd`, `table`, `download`）时，未开启特性时传入对应枚举必须返回 `AnyOcrError::UnsupportedFormat`，严禁 `panic` 或未定义行为；
- **格式支持与依赖对齐**：声明支持的格式（如 TIFF）必须在 `Cargo.toml` 中显式开启对应的解码 feature（如 `image` 的 `tiff`）。

### 5. 敏捷交付节奏准则
- **垂直闭环优先于过早优化**：
  - 阶段一必须先建立最小输入（图像解码）+ 最小推理（逐行 Baseline）+ 冒烟测试闭环；
  - 长宽比分桶（Bucket Batching）等高级算法必须在 Baseline 跑通后再行叠加，杜绝依赖悬空。

### 6. 轻量模型硬件加速与多核 CPU 权衡准则
- **反常识性能认知**：针对轻量级动态输入小模型（如 PP-OCR 文本行识别，H=48，动态 W），频繁跨进程搬运、动态 Graph Re-partition 与驱动开销往往远超计算本身。Apple Silicon 等多核 CPU（常驻高速缓存 + SIMD 向量化）吞吐可能显著优于 CoreML/GPU。绝不能盲目预设 NPU/GPU 必然更快，必须以多核 CPU 为零依赖稳定基线，将硬件加速严格 Feature 隔离。

### 7. 版式图像输入分辨率与自适应钳制黄金律 (Smart Clamping)
- **2560px 黄金上限**：A4 标准发票 8pt 密集小字在 ~220 DPI (最长边 2560px) 下物理高度约为 24px，进入 48px 识别模型处于约 2 倍双线性插值低失真采样区间，且 2560 可整除 32（严格对齐 DBNet 下采样步长）；
- **内存二次方红线**：严禁允许任意超大图（3000px~6000px）直接进入预处理，防止内存 $O(N^2)$ 暴增与无谓超采；
- **空间坐标绝对还原**：内部自适应降采样后，对外输出前必须执行等比逆变换还原回原始物理坐标，严禁污染 `PageResult.dimensions` 与前端画框高亮。

### 8. 多语言与小语种解耦准则
- **视觉感知语言无关性**：文本检测与表格拓扑必须保持 Language-Agnostic；全球 80+ 小语种（韩/俄/泰/阿等）扩展仅在识别模型与字典维度解耦，通过 `ModelProfile::Custom` 即插即用，杜绝破坏公共管线。

